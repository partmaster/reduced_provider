name: reduced_provider
description: reduced for provider.
version: 0.1.0
homepage:
publish_to: none

environment:
  sdk: '>=2.17.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  reduced:
    git:
      url: https://github.com/partmaster/reduced.git
      ref: main
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
