name: reduced_provider
version: 0.1.0
description: Implementation of the state management API 'reduced' on top of the state management framework 'Provider'.
homepage: https://github.com/partmaster/reduced_provider
repository: https://github.com/partmaster/reduced_provider.git
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  reduced:
    git:
      url: https://github.com/partmaster/reduced.git
      ref: main
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
